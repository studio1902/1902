title: Quote
fields:
  -
    handle: quote
    field: common.entry
    config:
      display: Quote
      collections:
        - quotes
      validate:
        - required
  -
    import: background_text
