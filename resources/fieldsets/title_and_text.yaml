title: 'Title and text'
fields:
  -
    handle: title
    field:
      type: text
      display: Title
      validate: required
      localizable: true
  -
    handle: tag
    field:
      options:
        h1: H1
        h2: H2
      default: h1
      display: 'Render as'
      type: button_group
      icon: button_group
      listable: hidden
      instructions_position: above
      read_only: false
      validate:
        - required
  -
    handle: introduction
    field: common.text_basic
    config:
      display: Text
      validate: required
      localizable: true
  -
    import: animation
  -
    import: background_text
